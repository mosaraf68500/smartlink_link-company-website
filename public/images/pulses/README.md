Place final product images for **/pulses** here — see SMARTLINK_PROJECT.md.

Once real files are added (e.g. `1.jpg`, `2.jpg`, `3.jpg`), update the
corresponding entry in `src/components/products/data/productImages.ts`
to point at `/images/pulses/1.jpg` etc. instead of the temporary Unsplash
demo URLs. No component code changes are needed.
